import Type from "../atom/Type";

const Card = () => {
  return (
    <div style={{ display: "flex" }}>
      <Type text="덴트" />
      <Type text="판금" />
      <Type text="교환" />
    </div>
  );
};

export default Card;
