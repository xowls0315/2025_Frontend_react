const Type = (props) => {
  return (
    <div
      style={{
        padding: "15px 30px",
        fontWeight: "bold",
        border: "1px solid green",
      }}
    >
      {props.text}
    </div>
  );
};

export default Type;
